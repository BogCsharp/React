const Loading = () => {
    return (
        <div style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            height: '100px'
        }}>
            <div>Загрузка...</div>
        </div>
    );
};

export default Loading;